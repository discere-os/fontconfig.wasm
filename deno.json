{
  "name": "@discere-os/fontconfig.wasm",
  "exports": {
    ".": "./",
    "./side": "./install/wasm/fontconfig-side.wasm",
    "./main": "./install/wasm/fontconfig-main.js"
  },
  "tasks": {
    "build:main:meson": "PKG_CONFIG_PATH=$PWD/../freetype.wasm/install/wasm/pkgconfig:$PWD/../libexpat.wasm/install/wasm/pkgconfig meson setup build-main --cross-file=scripts/emscripten.cross -Ddefault_library=static -Dtests=disabled -Dbuildtype=release --prefix=$PWD/install -Dlibdir=wasm -Dbindir=wasm && meson compile -C build-main fontconfig-main && meson install -C build-main",
    "build:side:meson": "PKG_CONFIG_PATH=$PWD/../freetype.wasm/install/wasm/pkgconfig:$PWD/../libexpat.wasm/install/wasm/pkgconfig meson setup build-side --cross-file=scripts/emscripten.cross -Ddefault_library=static -Dtests=disabled -Dbuildtype=release --prefix=$PWD/install -Dlibdir=wasm -Dbindir=wasm -Dc_args='-msimd128' -Dc_link_args='-sSIDE_MODULE=2 -fPIC -O3 -flto' && meson compile -C build-side fontconfig-side && meson install -C build-side && python3 scripts/postinstall.py $PWD/install",
    "build:wasm:meson": "deno task build:main:meson && deno task build:side:meson && deno task manifest",
    "manifest": "deno run --allow-read --allow-write --allow-run ../../../../scripts/generate-wasm-manifest.ts ."
  }
}
